# Live scorecard sharing (Supabase). Optional — if unset, the app works as
# before (local only) and the "Share Live" button is hidden.
# Find these in Supabase: Project Settings -> API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
